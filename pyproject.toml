[project]
name = "mng"
version = "0.2.0"
description = "My solutions for https://mng.quest"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["-s"]
